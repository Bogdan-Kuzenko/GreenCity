language: java
dist: trusty

addons:
  sonarcloud:
    organization: "bogdan"
    token:
      secure: "eddb87be43f9d05e9a92b4247b980d4a86a7bc52" # encrypted value of your token
          
jdk:
  - oraclejdk8
       


script:
  # the following command line builds the project, runs the tests with coverage and then execute the SonarCloud analysis
  - mvn clean org.jacoco:jacoco-maven-plugin:prepare-agent install sonar:sonar -Dsonar.projectKey=test_GreenCity1 -DskipTests=true
